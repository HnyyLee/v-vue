let setpSchedule = [ 'setpSchedule', { rules: [ { required: true, message: '请选择进度' } ] } ]

let stepValue = [ 'stepValue', { rules: [ { required: false, message: '请输入说明情况' } ] } ]

let obj = {
  setpSchedule,
  stepValue
}

export default obj
